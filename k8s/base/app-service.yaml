apiVersion: v1
kind: Service
metadata:
  name: catalogo-app-service
  namespace: oficina-ns
  labels:
    app: catalogo-app
spec:
  type: ClusterIP
  selector:
    app: catalogo-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 9091
