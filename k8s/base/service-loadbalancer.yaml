apiVersion: v1
kind: Service
metadata:
  name: catalogo-app-loadbalancer
  namespace: oficina-ns
  labels:
    app: catalogo-app
spec:
  type: LoadBalancer
  selector:
    app: catalogo-app
  ports:
  - protocol: TCP
    port: 80
    targetPort: 9091
